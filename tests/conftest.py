"""Shared pytest fixtures.

Per TRD §11: all Groq API calls must be mocked in tests — no test may
require a live GROQ_API_KEY. Add mocked-Groq-response fixtures here
in Phase 3 so every later test suite (Planner, Executor, Quality,
SelfHealing) can reuse them without hitting the real API.
"""

# TODO (Phase 3): add mocked Groq response fixtures
