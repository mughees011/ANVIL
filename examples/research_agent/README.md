# Research Agent (Example)

Demonstrates Anvil's memory + planning loop on a research task.

**Tool:** `wikipedia_search` — queries the Wikipedia API and returns snippets from the top 3 matching articles.

Run with:
```bash
anvil run research_agent "What caused the 2008 financial crisis?"
```
